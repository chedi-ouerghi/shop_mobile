const AlcoholDefinitionSection = () => {
  // const animatedStyle = useAnimatedStyle(() => {
  //   const inputRange = [0, 100];
  //   const translateX = interpolate(scrollY.value, inputRange, [-width, 0], 'clamp');
  //   const opacity = interpolate(scrollY.value, inputRange, [0, 1], 'clamp');
  //   return {
  //     transform: [{ translateX }],
  //     opacity,
  //   };
  // });

// const AnimatedCard = ({ alcohol, index, scrollX, width, navigation }) => {
//   const animatedStyle = useAnimatedStyle(() => {
//     const inputRange = [(index - 1) * width, index * width, (index + 1) * width];
//     const scale = interpolate(scrollX.value, inputRange, [0.8, 1, 0.8]);
//     return {
//       transform: [{ scale }],
//     };
//   });

//   return (
//     <Animated.View style={[styles.card, animatedStyle]}>
//       <ImageBackground source={{ uri: alcohol.url }} style={styles.backgroundImage}>
//         <Card
//           title={alcohol.titre}
//           description={alcohol.description}
//           price={alcohol.prix}
//           dispo={alcohol.dispo ? 'Disponible' : 'Non disponible'}
//           qte={alcohol.qte}
//           url={alcohol.url}
//         />
//         <View style={styles.buttonContainer}>
//           <Button
//             title="Voir les détails"
//             onPress={() => navigation.navigate('AlcoholDetails', { id: alcohol.id })}
//           />
//         </View>
//       </ImageBackground>
//     </Animated.View>
//   );
// };



 {/* Alcohol Cards Section */}
      {/* <View style={styles.cardSection}>
        <ScrollView
          horizontal
          pagingEnabled
          showsHorizontalScrollIndicator={false}
          scrollEventThrottle={16}
          style={styles.cardContainer}
          snapToInterval={width}
          decelerationRate="fast"
          onScroll={handleScrollX}
        >
          {alcoholData.map((alcohol, index) => (
            <AnimatedCard key={alcohol.id} alcohol={alcohol} index={index} scrollX={scrollX} width={width} navigation={navigation} />
          ))}
        </ScrollView>
        <TouchableOpacity
          style={styles.seeMoreButton}
          onPress={() => navigation.navigate('Alcohol')}
        >
          <Text style={styles.seeMoreText}>Voir plus</Text>
        </TouchableOpacity>
      </View>
       */}

       